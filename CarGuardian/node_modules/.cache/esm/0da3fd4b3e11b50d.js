let User;_3e1‍.x([["default",()=>_3e1‍.o]]);_3e1‍.w("../schema/UserSchema",[["default",["User"],function(v){User=v}]]);

const userInfo = async(req, res) => {
    const {id} = req.body;

    const reqUser = await User.findOne({ _id: id})

    if(!reqUser){   
        res.status(404).json({
            error: true,
            message: "User not found"
        })
    }else{
        res.status(200).json({
            error: false,
            message: "User found",
            user: reqUser
        })
    }
} 


_3e1‍.d(userInfo);